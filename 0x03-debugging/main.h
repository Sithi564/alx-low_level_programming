#ifndef MAIN_H
#define MAIN_H

/**
 * Positive_or_negative-tests to see if integer value is positive or negative
 * returns number sign
 */

void positive_or_negative(int);

/**
 * largest_number-checks for the largest number out of 3 arguments passed to it
 * returns the largest number
 */

int largest_number(int a, int b, int c);

/**
 * print_remaining_days-Prints the remaining days of a year as well as the current day.
 * returns current day in a year as well as the number of days remaining in said year.
 */

print_remaining_days();

#endif  /* !defined(alx_low_level_ptogramming... 0x02-debugging) */
